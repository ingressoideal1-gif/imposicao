# QR Ideal — Plano de Implementação (Parte 1)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** O ingresso sai da impressora com um código de 8 caracteres que ninguém adivinha, calculado a partir do pedido, do modelo e do número do item.

**Architecture:** Um módulo novo `qr_ideal.py` isola a fórmula e a leitura do pool binário; o `engine.py` ganha um ramo `QR_IDEAL` que só consome esse módulo; o frontend ganha o elemento e a prévia servida pelo agente local. O pool de 24 MB fica ao lado do `NewProd.exe`, nunca no git e nunca na nuvem.

**Tech Stack:** Python 3 (stdlib + PyMuPDF + `qrcode`, todos já no `requirements.txt`), JavaScript vanilla, FastAPI, pytest.

**Spec:** [2026-08-13-qr-ideal-design.md](../specs/2026-08-13-qr-ideal-design.md)

## Global Constraints

- **Nenhuma dependência nova.** `openpyxl` e `pandas` não estão no `requirements.txt` e não entram: o `.xlsx` é lido com `zipfile` + `xml.etree.ElementTree` da biblioteca padrão.
- **O prefixo do QR é string do começo ao fim.** Pedido `20270` vira `07202`. Nunca converter para `int`.
- **O pool nunca entra no git.** `Ideal Control/` e `*.xlsx` já estão no `.gitignore`; `*.bin` entra na Task 1.
- **Nenhuma tabela é criada ou alterada** nesta parte.
- **Constantes do pool:** 100 colunas × 30.000 linhas = 3.000.000 códigos, 8 bytes cada, gravados coluna a coluna.
- **Valor canônico de conferência:** pedido `20272`, modelo `1000022`, item `7` → coluna `50`, índice `1470006`, código `HM4IKCBY`, conteúdo `27202HM4IKCBY`.
- **Publicação:** mexer em `engine.py`, `app.py` ou `frontend/` obriga a publicar o agente na mesma leva, com número de versão novo.

---

### Task 1: O módulo `qr_ideal.py` — fórmula e leitura do pool

**Files:**
- Create: `qr_ideal.py`
- Create: `tests/test_qr_ideal.py`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: nada.
- Produces: `ultimos2(n) -> int`, `coluna_do_modelo(pedido, modelo) -> int`, `indice(pedido, modelo, item) -> int`, `caminho_padrao() -> str`, e a classe `PoolQR(caminho)` com os métodos `codigo(pedido, modelo, item) -> str`, `conteudo(pedido, modelo, item) -> str` e `fechar()`. Constantes `COLUNAS = 100`, `LINHAS = 30000`, `TAMANHO = 8`, `TOTAL = 3000000`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/test_qr_ideal.py`:

```python
"""A fórmula e o pool do QR Ideal.

O pool real tem 24 MB e não está no git. Estes testes montam um pool
sintético de tamanho completo mas conteúdo previsível: o código da posição
`idx` é o próprio `idx` em base 36, preenchido até 8 caracteres. Assim dá
para conferir qualquer célula sem carregar o arquivo de produção.
"""
import os
import tempfile

import pytest

import qr_ideal


def _base36(n: int) -> str:
    alfabeto = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    s = ""
    while n:
        n, r = divmod(n, 36)
        s = alfabeto[r] + s
    return (s or "0").rjust(qr_ideal.TAMANHO, "0")


@pytest.fixture(scope="module")
def pool_sintetico():
    """Pool completo (3.000.000 × 8 bytes) onde pool[idx] == base36(idx)."""
    fd, caminho = tempfile.mkstemp(suffix=".bin")
    os.close(fd)
    with open(caminho, "wb") as f:
        for bloco in range(0, qr_ideal.TOTAL, 100_000):
            fim = min(bloco + 100_000, qr_ideal.TOTAL)
            f.write(b"".join(_base36(i).encode("ascii") for i in range(bloco, fim)))
    yield caminho
    os.unlink(caminho)


def test_ultimos2_pega_os_dois_ultimos_digitos():
    assert qr_ideal.ultimos2(20272) == 72
    assert qr_ideal.ultimos2(1000022) == 22
    assert qr_ideal.ultimos2("20270") == 70
    assert qr_ideal.ultimos2(1000000) == 0


def test_coluna_do_exemplo_canonico():
    # 72 - 22 = 50. É o exemplo que o usuário deu e que foi conferido na planilha.
    assert qr_ideal.coluna_do_modelo(20272, 1000022) == 50


def test_coluna_de_diferenca_negativa_nao_sai_da_faixa():
    # 22 - 72 = -50, que sem o mod 100 não seria coluna nenhuma.
    assert qr_ideal.coluna_do_modelo(20222, 1000072) == 50


def test_coluna_de_diferenca_zero_vira_100():
    # A subtração crua nunca alcança 100 (o máximo é 99); o zero ocupa esse lugar.
    assert qr_ideal.coluna_do_modelo(20222, 1000022) == 100


def test_coluna_fica_sempre_entre_1_e_100():
    for pedido in range(20200, 20300):
        for modelo in range(1000000, 1000100):
            c = qr_ideal.coluna_do_modelo(pedido, modelo)
            assert 1 <= c <= 100


def test_indice_do_exemplo_canonico():
    # coluna 50, item 7 -> (50-1)*30000 + 6
    assert qr_ideal.indice(20272, 1000022, 7) == 1_470_006


def test_item_acima_de_30000_avanca_para_a_coluna_seguinte():
    # O ingresso 30.001 da coluna 50 é a linha 1 da coluna 51.
    assert qr_ideal.indice(20272, 1000022, 30_001) == qr_ideal.indice(20272, 1000022, 1) + qr_ideal.LINHAS


def test_fim_do_pool_volta_para_o_comeco():
    # Coluna 100, item 30.001 passaria de 3.000.000: dá a volta.
    assert qr_ideal.indice(20222, 1000022, 30_001) == 0


def test_codigo_le_a_posicao_certa_do_pool(pool_sintetico):
    pool = qr_ideal.PoolQR(pool_sintetico)
    assert pool.codigo(20272, 1000022, 7) == _base36(1_470_006)
    pool.fechar()


def test_conteudo_inverte_o_pedido_e_cola_o_codigo(pool_sintetico):
    pool = qr_ideal.PoolQR(pool_sintetico)
    esperado = "27202" + _base36(1_470_006)
    assert pool.conteudo(20272, 1000022, 7) == esperado
    pool.fechar()


def test_pedido_terminado_em_zero_mantem_o_zero_a_esquerda(pool_sintetico):
    # 20270 invertido é "07202". Tratar como número destruiria o pedido.
    pool = qr_ideal.PoolQR(pool_sintetico)
    conteudo = pool.conteudo(20270, 1000022, 1)
    assert conteudo.startswith("07202")
    assert len(conteudo) == 5 + qr_ideal.TAMANHO
    pool.fechar()


def test_pool_de_tamanho_errado_e_recusado():
    fd, caminho = tempfile.mkstemp(suffix=".bin")
    os.close(fd)
    with open(caminho, "wb") as f:
        f.write(b"CURTO123")
    try:
        with pytest.raises(ValueError, match="tamanho"):
            qr_ideal.PoolQR(caminho)
    finally:
        os.unlink(caminho)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `python -m pytest tests/test_qr_ideal.py -q`
Expected: FAIL com `ModuleNotFoundError: No module named 'qr_ideal'`

- [ ] **Step 3: Escrever o módulo**

Criar `qr_ideal.py` na raiz do projeto (mesma pasta de `engine.py` e `db.py`, seguindo a convenção de módulos achatados):

```python
"""QR Ideal — o código que a portaria lê no ingresso.

Este módulo é a única fonte da regra. Ele não conhece PDF, não conhece
FastAPI e não conhece o editor: recebe (pedido, modelo, item) e devolve a
string que vai gravada no QR. Quem imprime, quem mostra na tela e quem
conferir amanhã leem daqui, e por isso a regra não pode divergir entre eles.

A regra:

    d      = (dois últimos dígitos do pedido − dois últimos do modelo) mod 100
    coluna = 100 se d == 0, senão d
    idx    = ((coluna − 1) × 30.000 + (item − 1)) mod 3.000.000
    código = pool[idx]

O `mod 100` existe porque a subtração crua vai de −99 a 99: metade das
combinações não teria coluna, e a coluna 100 seria inalcançável.

O `mod 3.000.000` é a fita contínua: o ingresso 30.001 cai naturalmente na
linha 1 da coluna seguinte, sem caso especial.
"""
import os
import sys

COLUNAS = 100
LINHAS = 30_000
TAMANHO = 8
TOTAL = COLUNAS * LINHAS  # 3.000.000

NOME_ARQUIVO = "qr_ideal_pool.bin"


def ultimos2(n) -> int:
    """Os dois últimos dígitos de um número, como inteiro."""
    return int(str(n).strip()[-2:])


def coluna_do_modelo(pedido, modelo) -> int:
    """A coluna do pool que atende este modelo dentro deste pedido (1..100)."""
    d = (ultimos2(pedido) - ultimos2(modelo)) % 100
    return COLUNAS if d == 0 else d


def indice(pedido, modelo, item: int) -> int:
    """A posição do código no pool, contando coluna a coluna a partir de zero."""
    col = coluna_do_modelo(pedido, modelo)
    return ((col - 1) * LINHAS + (int(item) - 1)) % TOTAL


def prefixo(pedido) -> str:
    """O número do pedido de trás para frente.

    String sempre: o pedido 20270 vira "07202", e convertê-lo para inteiro o
    transformaria em 7202 — que invertido é outro pedido.
    """
    return str(pedido).strip()[::-1]


def caminho_padrao() -> str:
    """Onde o pool mora: ao lado do executável na estação, na raiz em dev."""
    if getattr(sys, "frozen", False):
        base = os.path.dirname(sys.executable)
    else:
        base = os.path.dirname(os.path.abspath(__file__))
    return os.path.join(base, NOME_ARQUIVO)


class PoolQR:
    """Leitura do pool por posição direta, com o arquivo aberto uma vez só.

    Uma tiragem de 10.000 ingressos faz 10.000 leituras; abrir e fechar o
    arquivo a cada uma seria desperdício num caminho que o operador espera
    de pé na frente da impressora.
    """

    def __init__(self, caminho: str | None = None):
        self.caminho = caminho or caminho_padrao()
        if not os.path.exists(self.caminho):
            raise FileNotFoundError(
                f"Pool do QR Ideal nao encontrado em {self.caminho}. "
                "Sem ele nao ha como imprimir QR Ideal."
            )
        tamanho = os.path.getsize(self.caminho)
        if tamanho != TOTAL * TAMANHO:
            raise ValueError(
                f"Pool do QR Ideal com tamanho errado: {tamanho} bytes, "
                f"esperado {TOTAL * TAMANHO}."
            )
        self._f = open(self.caminho, "rb")

    def codigo(self, pedido, modelo, item: int) -> str:
        """Os 8 caracteres do pool para este ingresso."""
        idx = indice(pedido, modelo, item)
        self._f.seek(idx * TAMANHO)
        bruto = self._f.read(TAMANHO)
        if len(bruto) != TAMANHO:
            raise ValueError(f"Leitura curta do pool na posicao {idx}.")
        return bruto.decode("ascii")

    def conteudo(self, pedido, modelo, item: int) -> str:
        """O que fica gravado no QR: pedido invertido + código, sem separador."""
        return prefixo(pedido) + self.codigo(pedido, modelo, item)

    def fechar(self):
        if getattr(self, "_f", None):
            self._f.close()
            self._f = None

    def __enter__(self):
        return self

    def __exit__(self, *exc):
        self.fechar()
```

- [ ] **Step 4: Proteger o `.bin` no git**

Editar `.gitignore`, no bloco do pool que já existe (procurar a linha `Ideal Control/`), acrescentando `*.bin` logo abaixo de `*.xlsx`:

```
Ideal Control/
*.xlsx
*.bin
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `python -m pytest tests/test_qr_ideal.py -q`
Expected: PASS, 12 testes.

- [ ] **Step 6: Commit**

```bash
git add qr_ideal.py tests/test_qr_ideal.py .gitignore
git commit -m "feat(qr-ideal): a formula e a leitura do pool"
```

---

### Task 2: O conversor da planilha para o pool binário

**Files:**
- Create: `ferramentas/converter_pool.py`
- Create: `tests/test_converter_pool.py`

**Interfaces:**
- Consumes: `qr_ideal.COLUNAS`, `qr_ideal.LINHAS`, `qr_ideal.TAMANHO`, `qr_ideal.TOTAL`.
- Produces: `converter(caminho_xlsx, caminho_bin) -> int` (devolve quantos códigos gravou) e `celulas(caminho_xlsx)` — gerador que devolve `(coluna, linha, codigo)` lendo o XML em fluxo.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/test_converter_pool.py`:

```python
"""O conversor do pool: xlsx -> bin, com as invariantes verificadas."""
import os
import tempfile
import zipfile

import pytest

import qr_ideal
from ferramentas import converter_pool

NS = "http://schemas.openxmlformats.org/spreadsheetml/2006/main"


def _xlsx_falso(caminho, linhas, colunas, codigo_de):
    """Monta um .xlsx mínimo com inlineStr, do jeito que o arquivo real é."""
    partes = ['<?xml version="1.0"?>', f'<worksheet xmlns="{NS}"><sheetData>']
    for lin in range(1, linhas + 1):
        partes.append(f'<row r="{lin}">')
        for col in range(1, colunas + 1):
            v = codigo_de(col, lin)
            partes.append(f'<c t="inlineStr"><is><t>{v}</t></is></c>')
        partes.append("</row>")
    partes.append("</sheetData></worksheet>")
    with zipfile.ZipFile(caminho, "w") as z:
        z.writestr("xl/worksheets/sheet1.xml", "".join(partes))


def test_converte_gravando_coluna_a_coluna(monkeypatch):
    # Pool pequeno para o teste: 3 colunas x 4 linhas.
    monkeypatch.setattr(qr_ideal, "COLUNAS", 3)
    monkeypatch.setattr(qr_ideal, "LINHAS", 4)
    monkeypatch.setattr(qr_ideal, "TOTAL", 12)

    fd_x, cx = tempfile.mkstemp(suffix=".xlsx")
    fd_b, cb = tempfile.mkstemp(suffix=".bin")
    os.close(fd_x)
    os.close(fd_b)
    try:
        _xlsx_falso(cx, 4, 3, lambda c, l: f"C{c}L{l}".ljust(8, "X"))
        n = converter_pool.converter(cx, cb)
        assert n == 12
        with open(cb, "rb") as f:
            dados = f.read()
        assert len(dados) == 12 * 8
        # Coluna a coluna: as 4 linhas da coluna 1 vêm primeiro.
        assert dados[0:8] == b"C1L1XXXX"
        assert dados[3 * 8:4 * 8] == b"C1L4XXXX"
        assert dados[4 * 8:5 * 8] == b"C2L1XXXX"
    finally:
        os.unlink(cx)
        os.unlink(cb)


def test_recusa_planilha_com_codigo_de_tamanho_errado(monkeypatch):
    monkeypatch.setattr(qr_ideal, "COLUNAS", 2)
    monkeypatch.setattr(qr_ideal, "LINHAS", 2)
    monkeypatch.setattr(qr_ideal, "TOTAL", 4)

    fd_x, cx = tempfile.mkstemp(suffix=".xlsx")
    fd_b, cb = tempfile.mkstemp(suffix=".bin")
    os.close(fd_x)
    os.close(fd_b)
    try:
        _xlsx_falso(cx, 2, 2, lambda c, l: "CURTO")
        with pytest.raises(ValueError, match="8 caracteres"):
            converter_pool.converter(cx, cb)
    finally:
        os.unlink(cx)
        os.unlink(cb)


def test_recusa_planilha_com_codigo_repetido(monkeypatch):
    monkeypatch.setattr(qr_ideal, "COLUNAS", 2)
    monkeypatch.setattr(qr_ideal, "LINHAS", 2)
    monkeypatch.setattr(qr_ideal, "TOTAL", 4)

    fd_x, cx = tempfile.mkstemp(suffix=".xlsx")
    fd_b, cb = tempfile.mkstemp(suffix=".bin")
    os.close(fd_x)
    os.close(fd_b)
    try:
        _xlsx_falso(cx, 2, 2, lambda c, l: "IGUALIGU")
        with pytest.raises(ValueError, match="repetido"):
            converter_pool.converter(cx, cb)
    finally:
        os.unlink(cx)
        os.unlink(cb)


def test_recusa_planilha_com_numero_de_celulas_errado(monkeypatch):
    monkeypatch.setattr(qr_ideal, "COLUNAS", 5)
    monkeypatch.setattr(qr_ideal, "LINHAS", 5)
    monkeypatch.setattr(qr_ideal, "TOTAL", 25)

    fd_x, cx = tempfile.mkstemp(suffix=".xlsx")
    fd_b, cb = tempfile.mkstemp(suffix=".bin")
    os.close(fd_x)
    os.close(fd_b)
    try:
        _xlsx_falso(cx, 2, 2, lambda c, l: f"C{c}L{l}".ljust(8, "X"))
        with pytest.raises(ValueError, match="esperado"):
            converter_pool.converter(cx, cb)
    finally:
        os.unlink(cx)
        os.unlink(cb)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `python -m pytest tests/test_converter_pool.py -q`
Expected: FAIL com `ModuleNotFoundError: No module named 'ferramentas'`

- [ ] **Step 3: Escrever o conversor**

Criar `ferramentas/__init__.py` vazio, e `ferramentas/converter_pool.py`:

```python
"""Converte o Ideal Control.xlsx no qr_ideal_pool.bin.

Roda uma vez, na máquina que monta o instalador do agente. A planilha tem
169 MB de XML e 3.000.000 de células; ler tudo na memória seria desperdício,
então o XML é percorrido em fluxo e o `.bin` é escrito em blocos.

O formato de saída é deliberadamente burro: 8 bytes por código, sem
separador, sem cabeçalho, gravados coluna a coluna. Ler o código de um
ingresso vira `seek(idx * 8)` e `read(8)` — nenhum parser no caminho de quem
está esperando o papel sair.

Uso:
    python -m ferramentas.converter_pool "Ideal Control/Ideal Control.xlsx" qr_ideal_pool.bin
"""
import sys
import zipfile
import xml.etree.ElementTree as ET

import qr_ideal

NS = "{http://schemas.openxmlformats.org/spreadsheetml/2006/main}"


def celulas(caminho_xlsx):
    """Percorre a planilha em fluxo, devolvendo (coluna, linha, codigo)."""
    z = zipfile.ZipFile(caminho_xlsx)
    with z.open("xl/worksheets/sheet1.xml") as f:
        linha = 0
        coluna = 0
        for evento, el in ET.iterparse(f, events=("start", "end")):
            if evento == "start" and el.tag == NS + "row":
                linha += 1
                coluna = 0
            elif evento == "end" and el.tag == NS + "c":
                coluna += 1
                bloco_is = el.find(NS + "is")
                if bloco_is is not None:
                    t = bloco_is.find(NS + "t")
                    texto = t.text if t is not None else ""
                else:
                    v = el.find(NS + "v")
                    texto = v.text if v is not None else ""
                yield coluna, linha, (texto or "").strip()
                el.clear()
            elif evento == "end" and el.tag == NS + "row":
                el.clear()


def converter(caminho_xlsx: str, caminho_bin: str) -> int:
    """Grava o pool binário e devolve quantos códigos entraram.

    Recusa gravar se qualquer invariante quebrar: código fora dos 8
    caracteres, código repetido, ou número de células diferente do esperado.
    Um pool errado só apareceria na portaria, quando não dá mais para
    consertar.
    """
    # A grade chega em ordem de linha; o arquivo sai em ordem de coluna.
    grade = [[None] * qr_ideal.LINHAS for _ in range(qr_ideal.COLUNAS)]
    vistos = set()
    total = 0

    for coluna, linha, codigo in celulas(caminho_xlsx):
        if len(codigo) != qr_ideal.TAMANHO:
            raise ValueError(
                f"Codigo com {len(codigo)} caracteres na coluna {coluna}, "
                f"linha {linha}: esperado 8 caracteres."
            )
        if codigo in vistos:
            raise ValueError(
                f"Codigo repetido {codigo!r} na coluna {coluna}, linha {linha}."
            )
        vistos.add(codigo)
        if coluna > qr_ideal.COLUNAS or linha > qr_ideal.LINHAS:
            raise ValueError(
                f"Celula fora da grade: coluna {coluna}, linha {linha}. "
                f"esperado ate {qr_ideal.COLUNAS}x{qr_ideal.LINHAS}."
            )
        grade[coluna - 1][linha - 1] = codigo
        total += 1

    if total != qr_ideal.TOTAL:
        raise ValueError(
            f"Planilha com {total} codigos, esperado {qr_ideal.TOTAL}."
        )

    with open(caminho_bin, "wb") as saida:
        for coluna in grade:
            saida.write(b"".join(c.encode("ascii") for c in coluna))

    return total


if __name__ == "__main__":
    if len(sys.argv) != 3:
        print(__doc__)
        sys.exit(1)
    n = converter(sys.argv[1], sys.argv[2])
    print(f"{n:,} codigos gravados em {sys.argv[2]}")
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `python -m pytest tests/test_converter_pool.py -q`
Expected: PASS, 4 testes.

- [ ] **Step 5: Gerar o pool de verdade e conferir a célula canônica**

```bash
python -m ferramentas.converter_pool "Ideal Control/Ideal Control.xlsx" qr_ideal_pool.bin
python -c "import qr_ideal; p = qr_ideal.PoolQR('qr_ideal_pool.bin'); print(p.conteudo(20272, 1000022, 7))"
```

Expected: `3,000,000 codigos gravados em qr_ideal_pool.bin` e depois `27202HM4IKCBY`.

Se a segunda linha não for exatamente `27202HM4IKCBY`, **parar**: a ordem de gravação está errada e todo o resto do plano fica sobre areia.

- [ ] **Step 6: Commit**

```bash
git add ferramentas/__init__.py ferramentas/converter_pool.py tests/test_converter_pool.py
git commit -m "feat(qr-ideal): conversor da planilha para o pool binario"
```

---

### Task 3: O elemento no motor

**Files:**
- Modify: `engine.py` — `ImpositionConfig.__init__` (~linha 358), o cálculo de `hw/hh` (~linha 842), o ramo de desenho (~linha 1133)
- Modify: `app.py:875` — passar `pedido` e `modelo`
- Create: `tests/test_engine_qr_ideal.py`

**Interfaces:**
- Consumes: `qr_ideal.PoolQR`, `qr_ideal.coluna_do_modelo`, `engine._generate_qr`.
- Produces: `ImpositionConfig(pedido=..., modelo=..., pool_qr=...)`; o elemento `{"type": "QR_IDEAL", "size_mm": 15, "color": "#000000"}`; e `ImpositionEngine._modelo_do_item(item_index, multi_map) -> str | None`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/test_engine_qr_ideal.py`:

```python
"""O QR Ideal no papel: o PDF carrega exatamente a string esperada.

A prova não usa decodificador de QR — nenhuma biblioteca nova entra por
causa de teste. Ela extrai a imagem que o motor embutiu no PDF e compara
byte a byte com o que `_generate_qr` produz para o conteúdo esperado. Se o
motor tivesse calculado outro código, os bytes divergiriam.
"""
import os
import sys
import tempfile

import fitz
import pytest

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import qr_ideal
from engine import ImpositionConfig, ImpositionEngine, _generate_qr


def _base36(n: int) -> str:
    alfabeto = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    s = ""
    while n:
        n, r = divmod(n, 36)
        s = alfabeto[r] + s
    return (s or "0").rjust(qr_ideal.TAMANHO, "0")


@pytest.fixture(scope="module")
def pool_sintetico():
    fd, caminho = tempfile.mkstemp(suffix=".bin")
    os.close(fd)
    with open(caminho, "wb") as f:
        for bloco in range(0, qr_ideal.TOTAL, 100_000):
            fim = min(bloco + 100_000, qr_ideal.TOTAL)
            f.write(b"".join(_base36(i).encode("ascii") for i in range(bloco, fim)))
    yield caminho
    os.unlink(caminho)


FORMATO = {
    "width_mm": 60, "height_mm": 40,
    "cols": 1, "rows": 1,
    "offset_x_mm": 0, "offset_y_mm": 0,
}
SAIDA = {"width_mm": 210, "height_mm": 297}


def _numeracao(elements):
    return {"elements": elements, "csv_data": None}


def _base_pdf():
    doc = fitz.open()
    doc.new_page(width=60 * 2.8346, height=40 * 2.8346)
    fd, caminho = tempfile.mkstemp(suffix=".pdf")
    os.close(fd)
    doc.save(caminho)
    doc.close()
    return caminho


def _imagens_do_pdf(caminho):
    doc = fitz.open(caminho)
    saida = []
    for pagina in doc:
        for info in pagina.get_images(full=True):
            saida.append(doc.extract_image(info[0])["image"])
    doc.close()
    return saida


def test_o_papel_carrega_o_conteudo_do_qr_ideal(pool_sintetico):
    base = _base_pdf()
    fd, saida_pdf = tempfile.mkstemp(suffix=".pdf")
    os.close(fd)
    try:
        cfg = ImpositionConfig(
            base_file=base, out_pdf=saida_pdf,
            formato=FORMATO, saida=SAIDA,
            numeracao=_numeracao([
                {"id": "e1", "type": "QR_IDEAL", "x_mm": 30, "y_mm": 20,
                 "size_mm": 15, "color": "#000000", "rotation": 0},
            ]),
            seq_start=7, seq_end=7,
            pedido=20272, modelo=1000022,
            pool_qr=qr_ideal.PoolQR(pool_sintetico),
        )
        ImpositionEngine(cfg).run()

        esperado = "27202" + _base36(1_470_006)
        assert _generate_qr(esperado, "#000000") in _imagens_do_pdf(saida_pdf)
    finally:
        for c in (base, saida_pdf):
            if os.path.exists(c):
                os.unlink(c)


def test_sem_pedido_o_trabalho_falha_em_vez_de_imprimir_em_branco(pool_sintetico):
    base = _base_pdf()
    fd, saida_pdf = tempfile.mkstemp(suffix=".pdf")
    os.close(fd)
    try:
        cfg = ImpositionConfig(
            base_file=base, out_pdf=saida_pdf,
            formato=FORMATO, saida=SAIDA,
            numeracao=_numeracao([
                {"id": "e1", "type": "QR_IDEAL", "x_mm": 30, "y_mm": 20,
                 "size_mm": 15, "color": "#000000", "rotation": 0},
            ]),
            seq_start=1, seq_end=1,
            pedido=None, modelo=None,
            pool_qr=qr_ideal.PoolQR(pool_sintetico),
        )
        with pytest.raises(ValueError, match="QR Ideal"):
            ImpositionEngine(cfg).run()
    finally:
        for c in (base, saida_pdf):
            if os.path.exists(c):
                os.unlink(c)


def test_reimpressao_da_celula_7_traz_o_codigo_do_item_7(pool_sintetico):
    """O código segue o número do item, nunca a posição na folha compactada."""
    base = _base_pdf()
    fd, saida_pdf = tempfile.mkstemp(suffix=".pdf")
    os.close(fd)
    try:
        cfg = ImpositionConfig(
            base_file=base, out_pdf=saida_pdf,
            formato=FORMATO, saida=SAIDA,
            numeracao=_numeracao([
                {"id": "e1", "type": "QR_IDEAL", "x_mm": 30, "y_mm": 20,
                 "size_mm": 15, "color": "#000000", "rotation": 0},
            ]),
            seq_start=1, seq_end=20,
            pedido=20272, modelo=1000022,
            pool_qr=qr_ideal.PoolQR(pool_sintetico),
            refazer_celulas=[7],
        )
        ImpositionEngine(cfg).run()

        esperado = "27202" + _base36(1_470_006)
        imagens = _imagens_do_pdf(saida_pdf)
        assert _generate_qr(esperado, "#000000") in imagens
        # E não o código do item 1, que é onde ele caiu na folha.
        nao_esperado = "27202" + _base36(1_470_000)
        assert _generate_qr(nao_esperado, "#000000") not in imagens
    finally:
        for c in (base, saida_pdf):
            if os.path.exists(c):
                os.unlink(c)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `python -m pytest tests/test_engine_qr_ideal.py -q`
Expected: FAIL com `TypeError: __init__() got an unexpected keyword argument 'pedido'`

- [ ] **Step 3: Acrescentar `pedido`, `modelo` e `pool_qr` ao `ImpositionConfig`**

Em `engine.py`, na assinatura de `ImpositionConfig.__init__` (~linha 383), depois de `refazer_celulas: list = None`:

```python
                 refazer_celulas: list = None,
                 pedido=None,
                 modelo=None,
                 pool_qr=None):
```

E no corpo, junto das outras atribuições diretas (~linha 395, depois de `self.block_depth = block_depth`):

```python
        # QR Ideal: as duas chaves que dão o código de cada ingresso.
        # `pedido` é `pedidos_modelos.id_int` e `modelo` é `pedidos_modelos.id`.
        # Guardados como STRING: o pedido 20270 invertido é "07202", e um int
        # perderia o zero e viraria outro pedido.
        self.pedido = str(pedido).strip() if pedido not in (None, "") else None
        self.modelo = str(modelo).strip() if modelo not in (None, "") else None
        self.pool_qr = pool_qr
```

- [ ] **Step 4: Acrescentar o ramo de tamanho**

Em `engine.py:842`, trocar:

```python
        elif t == "QR":
```

por:

```python
        elif t in ("QR", "QR_IDEAL"):
```

- [ ] **Step 5: Acrescentar o método que resolve o modelo do item**

Em `engine.py`, logo depois de `_get_camarote_params` (~linha 815):

```python
    def _modelo_do_item(self, item_index: int, multi_map: list = None):
        """O modelo (id de 7 digitos) deste item.

        Numa folha `multi_artes` cada arte é um modelo diferente — e como um
        modelo é um setor do evento, cada arte usa uma coluna diferente do
        pool. Fora desse caso, o modelo é o do trabalho inteiro.
        """
        if multi_map and item_index < len(multi_map):
            m = multi_map[item_index].get("modelo")
            if m not in (None, ""):
                return str(m).strip()
        return self.cfg.modelo
```

- [ ] **Step 6: Acrescentar o ramo de desenho**

Em `engine.py`, logo depois do bloco `elif t == "QR":` que termina em `page.insert_image(rect, stream=qr_bytes)` (~linha 1141), inserir:

```python
        elif t == "QR_IDEAL":
            # O conteúdo é calculado, nunca digitado: quem injeta é o chamador,
            # que é o único que sabe o modelo daquele item numa folha multi_artes.
            conteudo = el.get("_qr_ideal_conteudo")
            if not conteudo:
                raise ValueError(
                    "QR Ideal sem pedido ou modelo: o trabalho nao pode ser "
                    "impresso. Um QR em branco, ou calculado com valor suposto, "
                    "so apareceria na portaria — quando ja nao da para consertar."
                )
            size = el.get("_size", 42.5)
            qr_bytes = _generate_qr(conteudo, color)
            rect = fitz.Rect(el_x, el_y, el_x + size, el_y + size)
            py_rotate = (360 - angle) % 360
            if py_rotate != 0:
                page.insert_image(rect, stream=qr_bytes, rotate=py_rotate)
            else:
                page.insert_image(rect, stream=qr_bytes)
```

- [ ] **Step 7: Injetar o conteúdo nos quatro pontos de chamada**

Em `engine.py`, nas quatro chamadas de `self._render_element(...)` (linhas ~2138, ~2184, ~2387 e a de ~2650), inserir imediatamente **antes** de cada chamada:

```python
                            if rotated_el["type"] == "QR_IDEAL":
                                rotated_el["_qr_ideal_conteudo"] = self._conteudo_qr_ideal(
                                    item_index, current_val
                                )
```

E acrescentar o método, logo depois de `_modelo_do_item`:

```python
    def _conteudo_qr_ideal(self, item_index: int, val: int):
        """A string que vai gravada no QR deste ingresso, ou None se faltar dado.

        `val` é o número sequencial do item — o mesmo que a numeração imprime.
        Usar `val` e não a posição na folha é o que faz a reimpressão parcial
        sair certa: refazer a célula 7 imprime o código do item 7, mesmo que
        ele caia na primeira posição da folha compactada.
        """
        cfg = self.cfg
        multi = None
        if cfg.layout_schema == "multi_artes" or (cfg.multi_artes and len(cfg.multi_artes) > 0):
            multi = cfg.multi_artes
        modelo = self._modelo_do_item(item_index, multi)
        if not cfg.pedido or not modelo or cfg.pool_qr is None:
            return None
        return cfg.pool_qr.conteudo(cfg.pedido, modelo, val)
```

- [ ] **Step 8: Passar `pedido` e `modelo` pelo endpoint**

Em `app.py`, na construção do `ImpositionConfig` (linha ~899), depois de `refazer_celulas=data.get("refazer_celulas") or []`:

```python
            refazer_celulas=data.get("refazer_celulas") or [],
            pedido=data.get("pedido"),
            modelo=data.get("modelo"),
            pool_qr=_pool_qr_ou_none()
        )
```

E antes da função `impose_file`, o carregador preguiçoso:

```python
_POOL_QR = None


def _pool_qr_ou_none():
    """O pool do QR Ideal, aberto uma vez por processo.

    Devolve None quando o arquivo nao esta na maquina — o que e o normal no
    servidor da nuvem, que nao imprime. Quem cobra a falta e o ramo QR_IDEAL
    do motor, e so quando o trabalho realmente usa o elemento.
    """
    global _POOL_QR
    if _POOL_QR is None:
        try:
            import qr_ideal
            _POOL_QR = qr_ideal.PoolQR()
        except (FileNotFoundError, ValueError) as e:
            print(f"[qr-ideal] pool indisponivel: {e}")
            _POOL_QR = False
    return _POOL_QR or None
```

- [ ] **Step 9: Rodar e confirmar que passa**

Run: `python -m pytest tests/test_engine_qr_ideal.py -q`
Expected: PASS, 3 testes.

- [ ] **Step 10: Rodar a suíte inteira para provar que nada quebrou**

Run: `python -m pytest tests/ -q`
Expected: os testes que já passavam continuam passando.

- [ ] **Step 11: Commit**

```bash
git add engine.py app.py tests/test_engine_qr_ideal.py
git commit -m "feat(qr-ideal): o elemento QR_IDEAL no motor"
```

---

### Task 4: A trava das colunas repetidas em `multi_artes`

**Files:**
- Modify: `engine.py` — `_conteudo_qr_ideal` e um novo `_conferir_colunas_qr_ideal`
- Modify: `tests/test_engine_qr_ideal.py`

**Interfaces:**
- Consumes: `qr_ideal.coluna_do_modelo`, `ImpositionConfig.multi_artes`.
- Produces: `ImpositionEngine._conferir_colunas_qr_ideal()` — levanta `ValueError` se duas artes da mesma folha caírem na mesma coluna.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_engine_qr_ideal.py`:

```python
def test_dois_modelos_na_mesma_folha_usam_colunas_diferentes(pool_sintetico):
    # 1000022 -> coluna 50; 1000023 -> coluna 49.
    assert qr_ideal.coluna_do_modelo(20272, 1000022) == 50
    assert qr_ideal.coluna_do_modelo(20272, 1000023) == 49


def test_modelos_com_id_distante_100_sao_recusados(pool_sintetico):
    """1000022 e 1000122 caem os dois na coluna 50 — QRs idênticos no mesmo
    evento, o único choque que o prefixo do pedido não separa."""
    assert qr_ideal.coluna_do_modelo(20272, 1000022) == qr_ideal.coluna_do_modelo(20272, 1000122)

    base = _base_pdf()
    fd, saida_pdf = tempfile.mkstemp(suffix=".pdf")
    os.close(fd)
    try:
        cfg = ImpositionConfig(
            base_file=base, out_pdf=saida_pdf,
            formato=FORMATO, saida=SAIDA,
            numeracao=_numeracao([
                {"id": "e1", "type": "QR_IDEAL", "x_mm": 30, "y_mm": 20,
                 "size_mm": 15, "color": "#000000", "rotation": 0},
            ]),
            seq_start=1, seq_end=2,
            pedido=20272, modelo=1000022,
            pool_qr=qr_ideal.PoolQR(pool_sintetico),
            layout_schema="multi_artes",
            multi_artes=[{"modelo": "1000022"}, {"modelo": "1000122"}],
        )
        with pytest.raises(ValueError, match="mesma coluna"):
            ImpositionEngine(cfg).run()
    finally:
        for c in (base, saida_pdf):
            if os.path.exists(c):
                os.unlink(c)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `python -m pytest tests/test_engine_qr_ideal.py -q -k colunas`
Expected: FAIL — o segundo teste não levanta `ValueError`.

- [ ] **Step 3: Escrever a trava**

Em `engine.py`, logo depois de `_conteudo_qr_ideal`:

```python
    def _conferir_colunas_qr_ideal(self):
        """Duas artes da mesma folha nao podem cair na mesma coluna do pool.

        Modelos cujos `id` diferem em exatamente 100 dao a mesma coluna, e ai
        sairiam QRs IDENTICOS no mesmo evento — o unico choque que o prefixo
        do pedido nao separa. Melhor recusar o trabalho do que descobrir na
        portaria.
        """
        cfg = self.cfg
        if not cfg.pedido or not cfg.multi_artes:
            return
        import qr_ideal as _qi
        por_coluna = {}
        for arte in cfg.multi_artes:
            modelo = arte.get("modelo")
            if not modelo:
                continue
            col = _qi.coluna_do_modelo(cfg.pedido, modelo)
            anterior = por_coluna.get(col)
            if anterior is not None and str(anterior) != str(modelo):
                raise ValueError(
                    f"Modelos {anterior} e {modelo} caem na mesma coluna "
                    f"({col}) do pool do QR Ideal e produziriam ingressos com "
                    f"o mesmo codigo no mesmo evento. Trabalho recusado."
                )
            por_coluna[col] = modelo
```

E chamá-la uma vez, no começo de `run()`, logo depois da primeira linha do método:

```python
        self._conferir_colunas_qr_ideal()
```

> Só vale conferir quando algum elemento é `QR_IDEAL`. Se `run()` for grande demais para localizar o ponto, chamar a partir de `_conteudo_qr_ideal` na primeira invocação, guardando um `self._colunas_conferidas = True`.

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `python -m pytest tests/test_engine_qr_ideal.py -q`
Expected: PASS, 5 testes.

- [ ] **Step 5: Commit**

```bash
git add engine.py tests/test_engine_qr_ideal.py
git commit -m "feat(qr-ideal): recusar folha com dois modelos na mesma coluna"
```

---

### Task 5: O endpoint da prévia

**Files:**
- Modify: `app.py` — endpoint novo
- Create: `tests/test_api_qr_ideal.py`

**Interfaces:**
- Consumes: `_pool_qr_ou_none()` da Task 3.
- Produces: `GET /api/qr-ideal?pedido=&modelo=&item=` → `{"codigo", "conteudo", "coluna", "linha"}`, ou 503 quando não há pool na máquina.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/test_api_qr_ideal.py`:

```python
"""O endpoint que alimenta a prévia do editor.

Ele só existe onde o pool existe — quer dizer, na estação. No servidor da
nuvem ele responde 503, e é isso que faz a tela mostrar um QR de exemplo
avisado em vez de um QR falso mudo.
"""
import os
import sys
import tempfile

import pytest
from fastapi.testclient import TestClient

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import qr_ideal


def _base36(n: int) -> str:
    alfabeto = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    s = ""
    while n:
        n, r = divmod(n, 36)
        s = alfabeto[r] + s
    return (s or "0").rjust(qr_ideal.TAMANHO, "0")


@pytest.fixture(scope="module")
def pool_sintetico():
    fd, caminho = tempfile.mkstemp(suffix=".bin")
    os.close(fd)
    with open(caminho, "wb") as f:
        for bloco in range(0, qr_ideal.TOTAL, 100_000):
            fim = min(bloco + 100_000, qr_ideal.TOTAL)
            f.write(b"".join(_base36(i).encode("ascii") for i in range(bloco, fim)))
    yield caminho
    os.unlink(caminho)


def test_devolve_o_codigo_e_o_conteudo(pool_sintetico, monkeypatch):
    import app as app_mod
    monkeypatch.setattr(app_mod, "_POOL_QR", qr_ideal.PoolQR(pool_sintetico))
    cliente = TestClient(app_mod.app)

    r = cliente.get("/api/qr-ideal", params={"pedido": "20272", "modelo": "1000022", "item": 7})
    assert r.status_code == 200
    corpo = r.json()
    assert corpo["coluna"] == 50
    assert corpo["linha"] == 7
    assert corpo["codigo"] == _base36(1_470_006)
    assert corpo["conteudo"] == "27202" + _base36(1_470_006)


def test_sem_pool_na_maquina_responde_503(monkeypatch):
    import app as app_mod
    monkeypatch.setattr(app_mod, "_POOL_QR", False)
    cliente = TestClient(app_mod.app)

    r = cliente.get("/api/qr-ideal", params={"pedido": "20272", "modelo": "1000022", "item": 7})
    assert r.status_code == 503
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `python -m pytest tests/test_api_qr_ideal.py -q`
Expected: FAIL com `assert 404 == 200`

- [ ] **Step 3: Escrever o endpoint**

Em `app.py`, junto dos outros endpoints de consulta (perto de `/api/version`, ~linha 204):

```python
@app.get("/api/qr-ideal")
def qr_ideal_previa(pedido: str, modelo: str, item: int = 1):
    """O codigo do QR Ideal de um ingresso, para a previa do editor.

    Existe so onde o pool existe: na estacao. Na nuvem responde 503, e a
    tela desenha um QR de exemplo AVISADO — um QR falso mudo seria pior que
    nenhum, porque o operador acharia que conferiu.

    Sem autenticacao de propósito: quem alcanca este endereco ja esta dentro
    da estacao, e o painel do editor o chama a cada repintura. O que ele
    devolve e um unico codigo por vez, nunca a lista.
    """
    pool = _pool_qr_ou_none()
    if pool is None:
        raise HTTPException(
            status_code=503,
            detail="Pool do QR Ideal indisponivel nesta maquina."
        )
    import qr_ideal as _qi
    coluna = _qi.coluna_do_modelo(pedido, modelo)
    idx = _qi.indice(pedido, modelo, item)
    return {
        "codigo": pool.codigo(pedido, modelo, item),
        "conteudo": pool.conteudo(pedido, modelo, item),
        "coluna": coluna,
        "linha": (idx % _qi.LINHAS) + 1,
    }
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `python -m pytest tests/test_api_qr_ideal.py -q`
Expected: PASS, 2 testes.

- [ ] **Step 5: Commit**

```bash
git add app.py tests/test_api_qr_ideal.py
git commit -m "feat(qr-ideal): endpoint da previa servido pela estacao"
```

---

### Task 6: O elemento no editor

**Files:**
- Modify: `frontend/index.html:526` — botão novo
- Modify: `frontend/script.js` — onze ramificações: 3711, 3794, 4491, 5306, 5509, 5710, 8096, 15302, 24211, 24976, 29947

**Interfaces:**
- Consumes: `GET /api/qr-ideal`, `renderQRCodeOnCtx(ctx, text, x, y, sz, color, bgColor)` (definida em `frontend/script.js:3494`).
- Produces: `window.qrIdealConteudo(pedido, modelo, item) -> string|null` — cache síncrono alimentado por busca assíncrona; devolve `null` enquanto não souber.

- [ ] **Step 1: Acrescentar o botão**

Em `frontend/index.html`, logo depois da linha 526:

```html
                            <button class="btn btn-sm btn-secondary" onclick="addElement('QR_IDEAL')" title="QR de controle de acesso: o codigo vem do pedido, do modelo e do numero do ingresso — nao se digita">🎟️ QR Ideal</button>
```

- [ ] **Step 2: Acrescentar o helper de conteúdo**

Em `frontend/script.js`, logo depois de `renderQRCodeOnCtx` (linha ~3494 e o fim da função):

```javascript
/**
 * O conteúdo do QR Ideal de um ingresso, perguntado ao agente da estação.
 *
 * O desenho no canvas é síncrono e a consulta é assíncrona, então esta
 * função devolve `null` na primeira chamada, dispara a busca e repinta
 * quando a resposta chega. Fora da estação (Vercel) o endpoint não existe e
 * ela devolve `null` para sempre — quem desenha mostra um QR de exemplo com
 * aviso, nunca um QR falso mudo.
 */
const _qrIdealCache = new Map();
window.qrIdealConteudo = function (pedido, modelo, item) {
    if (!pedido || !modelo) return null;
    const chave = `${pedido}|${modelo}|${item || 1}`;
    if (_qrIdealCache.has(chave)) return _qrIdealCache.get(chave);
    _qrIdealCache.set(chave, null);
    fetch(`${API_BASE_URL}/api/qr-ideal?pedido=${encodeURIComponent(pedido)}&modelo=${encodeURIComponent(modelo)}&item=${item || 1}`)
        .then(r => r.ok ? r.json() : null)
        .then(j => {
            if (j && j.conteudo) {
                _qrIdealCache.set(chave, j.conteudo);
                if (typeof renderNumCanvas === 'function') renderNumCanvas();
            }
        })
        .catch(() => { });
    return null;
};

/** Desenha o QR Ideal, ou um quadro de exemplo que se anuncia como exemplo. */
window.desenharQRIdeal = function (ctx, el, sz, color) {
    const conteudo = window.qrIdealConteudo(
        state.qrIdealPedido, state.qrIdealModelo, el.ticket_pos || 1
    );
    if (conteudo) {
        renderQRCodeOnCtx(ctx, conteudo, 0, 0, sz, color);
        return;
    }
    const h = sz / 2;
    ctx.save();
    ctx.globalAlpha = 0.35;
    renderQRCodeOnCtx(ctx, 'EXEMPLO', 0, 0, sz, color);
    ctx.restore();
    ctx.fillStyle = '#f59e0b';
    ctx.font = `${Math.max(7, sz / 7)}px sans-serif`;
    ctx.textAlign = 'center';
    ctx.fillText('exemplo', 0, h + sz / 6);
};
```

- [ ] **Step 3: Ramificação 3711 — a amostra de texto**

Trocar:

```javascript
            el.type === 'QR' ? null :
```

por:

```javascript
            (el.type === 'QR' || el.type === 'QR_IDEAL') ? null :
```

- [ ] **Step 4: Ramificação 4491 — o tamanho do elemento**

Trocar:

```javascript
    else if (el.type === 'QR') { w = el.size_mm || 15; h = w; }
```

por:

```javascript
    else if (el.type === 'QR' || el.type === 'QR_IDEAL') { w = el.size_mm || 15; h = w; }
```

- [ ] **Step 5: Ramificação 5306 — os padrões do elemento novo**

Logo depois da linha `if (type === 'QR') Object.assign(...)`:

```javascript
    // QR Ideal: o conteúdo é calculado a partir de (pedido, modelo, item), e
    // nenhuma dessas três coisas se digita. Por isso não tem prefixo, sufixo,
    // zeros nem origem de dado — só tamanho e cor.
    if (type === 'QR_IDEAL') Object.assign(base, { size_mm: 15, render_mode: 'print' });
```

- [ ] **Step 6: Ramificação 5509 — o painel de propriedades**

Logo depois do bloco `} else if (el.type === 'QR') { ... }` que termina na linha 5525:

```javascript
        } else if (el.type === 'QR_IDEAL') {

            extraFields = `

                <div class="form-group el-full"><label>Tamanho (mm)</label><input class="form-control" type="number" value="${el.size_mm}" min="5" max="100" step="0.5" onchange="updateEl('${el.id}','size_mm',+this.value)"></div>

                <div class="form-group el-full" style="font-size:0.78rem; color:var(--text-dim); line-height:1.4;">
                    O código vem do <b>pedido</b>, do <b>modelo</b> e do <b>número do ingresso</b>.
                    Não há o que preencher aqui — e é isso que impede dois ingressos de saírem iguais.
                </div>`;
```

- [ ] **Step 7: Ramificação 5710 — a posição do ticket**

Trocar:

```javascript
        if (numTipoSelect && numTipoSelect.value === 'TICKET' && ['TEXT', 'QR', 'BARCODE'].includes(el.type)) {
```

por:

```javascript
        if (numTipoSelect && numTipoSelect.value === 'TICKET' && ['TEXT', 'QR', 'QR_IDEAL', 'BARCODE'].includes(el.type)) {
```

- [ ] **Step 8: Ramificações de desenho — 3794, 8096, 15302, 24211, 24976, 29947**

Em cada um dos seis pontos, acrescentar um `else if` **imediatamente antes** do `} else if (el.type === 'QR') {` correspondente, usando o `ctx` que aquele trecho já usa (`ctx` em 3794, 15302, 24211; `ctx` em 8096; `numCtx` em 24976 e 29947):

```javascript
        } else if (el.type === 'QR_IDEAL') {
            const sz = (el.size_mm || 15) * S;
            window.desenharQRIdeal(ctx, el, sz, color);
```

No ponto 8096 a escala é outra — usar `const sz = (el.size_mm || 15) * MM2PT * scale;`.
Nos pontos 24976 e 29947 trocar `ctx` por `numCtx` na chamada.

- [ ] **Step 9: Conferir os onze pontos**

Run: `rg -n "el\.type === 'QR'" frontend/script.js`

Para cada linha que sair, confirmar que existe um `QR_IDEAL` a menos de 20 linhas dela. Onze entradas, onze irmãos.

> Isso não é zelo: `SVG` e `PDF` foram acrescentados sem um ramo numa dessas funções e pintavam **zero pixel na tela** enquanto saíam certos no papel. O elemento existia, o dado existia, e a tela mentia.

- [ ] **Step 10: Ver na tela**

Subir o app e conferir de verdade — usar a skill `rodar-app` do projeto. Adicionar um QR Ideal no editor de numeração e confirmar: o elemento aparece, arrasta, redimensiona, e mostra o QR de exemplo com o aviso "exemplo" (a estação sem pool ainda não tem código).

- [ ] **Step 11: Commit**

```bash
git add frontend/index.html frontend/script.js
git commit -m "feat(qr-ideal): o elemento QR Ideal no editor de numeracao"
```

---

### Task 7: O aviso de colunas repetidas no painel do pedido

**Files:**
- Modify: `frontend/script.js` — onde a lista de modelos do pedido é montada (`frontend/script.js:17231`, o `select` de `pedidos_modelos`)
- Create: `tests/QrIdealColunas.Tests.ps1`

**Interfaces:**
- Consumes: `pedidos_modelos.id_int`, `pedidos_modelos.id`.
- Produces: `window.colunaQrIdeal(pedido, modelo) -> number` e `window.conferirColunasQrIdeal(pedido, modelos) -> Array<{coluna, modelos}>` (só os choques).

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/QrIdealColunas.Tests.ps1`, no padrão dos outros harness JS do projeto (ver `tests/CorNumeracaoDoModelo.Tests.ps1`):

```powershell
Describe "Colunas do QR Ideal no painel do pedido" {
    BeforeAll {
        $script:harness = Join-Path $PSScriptRoot "qr_ideal_colunas_harness.js"
    }

    It "calcula a coluna do exemplo canonico" {
        $saida = node $script:harness "coluna" "20272" "1000022"
        $saida.Trim() | Should -Be "50"
    }

    It "acusa dois modelos do mesmo pedido na mesma coluna" {
        $saida = node $script:harness "conferir" "20272" "1000022,1000122,1000023"
        $saida.Trim() | Should -Match "50"
    }

    It "nao acusa nada quando as colunas sao distintas" {
        $saida = node $script:harness "conferir" "20272" "1000022,1000023,1000024"
        $saida.Trim() | Should -Be "[]"
    }
}
```

E o harness `tests/qr_ideal_colunas_harness.js`:

```javascript
// Harness: roda as funções de coluna do QR Ideal fora do navegador.
const fs = require('fs');
const path = require('path');

const fonte = fs.readFileSync(path.join(__dirname, '..', 'frontend', 'qr-ideal-colunas.js'), 'utf8');
const window = {};
eval(fonte);

const [, , comando, pedido, arg] = process.argv;
if (comando === 'coluna') {
    console.log(window.colunaQrIdeal(pedido, arg));
} else {
    console.log(JSON.stringify(window.conferirColunasQrIdeal(pedido, arg.split(','))));
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `Invoke-Pester tests/QrIdealColunas.Tests.ps1`
Expected: FAIL — `frontend/qr-ideal-colunas.js` não existe.

- [ ] **Step 3: Escrever o módulo do frontend**

Criar `frontend/qr-ideal-colunas.js`:

```javascript
/**
 * A coluna do pool do QR Ideal, do lado do navegador.
 *
 * É a mesma regra do `qr_ideal.py`, e ela existe em dois lugares por um
 * motivo: o motor só enxerga os modelos de uma folha, e o painel é o único
 * que conhece o pedido inteiro. Se a regra divergir entre os dois, a tela
 * aprova um trabalho que a impressora recusa.
 */
window.colunaQrIdeal = function (pedido, modelo) {
    const u2 = (n) => parseInt(String(n).trim().slice(-2), 10);
    const d = ((u2(pedido) - u2(modelo)) % 100 + 100) % 100;
    return d === 0 ? 100 : d;
};

/**
 * Os choques entre os modelos de um pedido: mesma coluna, modelos
 * diferentes. Dois modelos cujos `id` diferem em exatamente 100 produzem
 * ingressos com o MESMO código no mesmo evento.
 */
window.conferirColunasQrIdeal = function (pedido, modelos) {
    const porColuna = new Map();
    (modelos || []).forEach((m) => {
        const c = window.colunaQrIdeal(pedido, m);
        if (!porColuna.has(c)) porColuna.set(c, []);
        porColuna.get(c).push(String(m));
    });
    const choques = [];
    porColuna.forEach((lista, coluna) => {
        if (lista.length > 1) choques.push({ coluna, modelos: lista });
    });
    return choques;
};
```

- [ ] **Step 4: Carregar o módulo na página**

Em `frontend/index.html`, junto dos outros `<script>` do fim do body (procurar `cor-numeracao-do-modelo.js`), acrescentar:

```html
    <script src="/qr-ideal-colunas.js?v=557"></script>
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `Invoke-Pester tests/QrIdealColunas.Tests.ps1`
Expected: PASS, 3 testes.

- [ ] **Step 6: Mostrar o aviso na tela**

Em `frontend/script.js`, onde os modelos do pedido são carregados (~linha 17231, o `.select('id, id_int, ...')` de `pedidos_modelos`), depois de os modelos estarem em memória:

```javascript
            // Dois modelos do mesmo pedido na mesma coluna do pool produzem
            // ingressos com o MESMO código no mesmo evento — o único choque
            // que o prefixo do pedido não separa. Avisar antes de imprimir.
            if (typeof window.conferirColunasQrIdeal === 'function' && modelos.length > 1) {
                const pedidoNum = modelos[0].id_int;
                const choques = window.conferirColunasQrIdeal(pedidoNum, modelos.map(m => m.id));
                if (choques.length) {
                    const texto = choques
                        .map(c => `coluna ${c.coluna}: modelos ${c.modelos.join(' e ')}`)
                        .join('; ');
                    showToast(
                        `⚠️ QR Ideal: ${texto}. Estes modelos gerariam ingressos com o mesmo código. ` +
                        `Não imprimir com QR Ideal antes de resolver.`,
                        'error'
                    );
                }
            }
```

- [ ] **Step 7: Commit**

```bash
git add frontend/qr-ideal-colunas.js frontend/index.html frontend/script.js tests/QrIdealColunas.Tests.ps1 tests/qr_ideal_colunas_harness.js
git commit -m "feat(qr-ideal): avisar no painel quando dois modelos caem na mesma coluna"
```

---

### Task 8: O pool chega às estações

**Files:**
- Modify: `build_agent.ps1`
- Modify: `installer.iss`
- Modify: `tests/Publicacao.Tests.ps1`

**Interfaces:**
- Consumes: `qr_ideal_pool.bin` produzido pela Task 2.
- Produces: o `.bin` instalado ao lado do `NewProd.exe`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/Publicacao.Tests.ps1`:

```powershell
Describe "Pool do QR Ideal na publicacao" {
    It "o build recusa seguir sem o pool" {
        $conteudo = Get-Content (Join-Path $PSScriptRoot ".." "build_agent.ps1") -Raw
        $conteudo | Should -Match "qr_ideal_pool\.bin"
        $conteudo | Should -Match "POOL_QR_IDEAL"
    }

    It "o instalador leva o pool para junto do executavel" {
        $conteudo = Get-Content (Join-Path $PSScriptRoot ".." "installer.iss") -Raw
        $conteudo | Should -Match "qr_ideal_pool\.bin"
    }

    It "o pool nao esta versionado" {
        $rastreados = git ls-files "*.bin"
        $rastreados | Should -BeNullOrEmpty
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `Invoke-Pester tests/Publicacao.Tests.ps1`
Expected: FAIL nos dois primeiros.

- [ ] **Step 3: Fazer o build exigir o pool**

Em `build_agent.ps1`, antes da chamada do PyInstaller:

```powershell
# O pool do QR Ideal nao esta no git — sao 24 MB e e o segredo mestre do
# controle de acesso. Ele vem de um caminho combinado fora do repositorio,
# apontado por POOL_QR_IDEAL, e o build PARA se ele nao estiver la: um agente
# publicado sem pool imprime ingresso sem codigo, e isso so aparece na portaria.
$poolOrigem = if ($env:POOL_QR_IDEAL) { $env:POOL_QR_IDEAL } else { Join-Path $PSScriptRoot "qr_ideal_pool.bin" }
if (-not (Test-Path $poolOrigem)) {
    Write-Host "ERRO: qr_ideal_pool.bin nao encontrado em $poolOrigem" -ForegroundColor Red
    Write-Host "      Gere com: python -m ferramentas.converter_pool `"Ideal Control/Ideal Control.xlsx`" qr_ideal_pool.bin" -ForegroundColor Yellow
    Write-Host "      Ou aponte POOL_QR_IDEAL para o arquivo." -ForegroundColor Yellow
    exit 1
}
$tamanhoEsperado = 24000000
$tamanhoReal = (Get-Item $poolOrigem).Length
if ($tamanhoReal -ne $tamanhoEsperado) {
    Write-Host "ERRO: qr_ideal_pool.bin com $tamanhoReal bytes, esperado $tamanhoEsperado." -ForegroundColor Red
    exit 1
}
Copy-Item $poolOrigem (Join-Path $PSScriptRoot "dist" "qr_ideal_pool.bin") -Force
```

> A cópia para `dist/` acontece **depois** do PyInstaller, se `dist/` for criado por ele. Ajustar a ordem conforme o script real.

- [ ] **Step 4: Fazer o instalador levar o pool**

Em `installer.iss`, na seção `[Files]`, junto do `NewProd.exe`:

```
Source: "dist\qr_ideal_pool.bin"; DestDir: "{app}"; Flags: ignoreversion
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `Invoke-Pester tests/Publicacao.Tests.ps1`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add build_agent.ps1 installer.iss tests/Publicacao.Tests.ps1
git commit -m "build(qr-ideal): o pool viaja com o instalador do agente"
```

---

### Task 9: Documentação e fechamento

**Files:**
- Create: `docs/qr_ideal.md`
- Modify: `CHANGELOG.md`
- Create: `.claude/skills/qr-ideal/SKILL.md`

- [ ] **Step 1: Escrever a documentação do recurso**

Criar `docs/qr_ideal.md` cobrindo, em prosa: o que o QR Ideal é e por que o `QR` antigo não serve para portão; a regra da coluna com o exemplo canônico conferido; o formato do conteúdo e a regra do prefixo como string; onde o pool mora e como regenerá-lo; as três travas; e os dois riscos aceitos (códigos compartilhados entre eventos, e o pedido invertido não ser assinatura).

- [ ] **Step 2: Escrever a skill**

Criar `.claude/skills/qr-ideal/SKILL.md`, no padrão das skills que já existem (`fotos-variaveis`, `elementos-pdf-svg`): quando ler, e as armadilhas do caminho — as onze ramificações do `script.js`, o prefixo que não pode virar `int`, o pool fora do git, a reimpressão parcial que usa o número do item, e a obrigação de publicar o agente junto.

- [ ] **Step 3: Registrar no CHANGELOG**

Acrescentar uma entrada no topo do `CHANGELOG.md`, seguindo o tom das entradas existentes: o problema (o QR atual é adivinhável), a regra nova, o que foi conferido contra a planilha real, e o que ficou de fora (partes 2 e 3).

- [ ] **Step 4: Rodar a suíte inteira**

Run: `python -m pytest tests/ -q`
Expected: tudo verde.

Run: `.\ferramentas\conferir.ps1`
Expected: sem segredo versionado, sem `.bin` e sem `.xlsx` rastreados.

- [ ] **Step 5: Commit**

```bash
git add docs/qr_ideal.md CHANGELOG.md .claude/skills/qr-ideal/SKILL.md
git commit -m "docs(qr-ideal): documentacao, skill e changelog"
```

- [ ] **Step 6: Avisar sobre a publicação**

Não publicar. Avisar o usuário de que `engine.py`, `app.py` e `frontend/` mudaram, e que por isso a publicação do site exige publicar o agente na mesma leva, com número de versão **novo** — e que as estações precisam receber o `qr_ideal_pool.bin` pelo instalador antes de qualquer trabalho com QR Ideal.

---

## Auto-revisão

**Cobertura da spec:** a regra da coluna (Task 1), a fita contínua (Task 1), o conteúdo com prefixo string (Task 1), o pool binário e o conversor (Tasks 1 e 2), o pool ao lado do exe (Tasks 1 e 8), o elemento no motor (Task 3), o modelo por arte em `multi_artes` (Task 3), a prévia (Tasks 5 e 6), o elemento no editor com os onze pontos (Task 6), a trava 1 nos dois lugares (Tasks 4 e 7), a trava 2 — pedido ausente (Task 3), a trava 3 — reimpressão parcial (Task 3), os sete testes da spec (Tasks 1 a 5), e a publicação (Tasks 8 e 9).

**Um ponto que a spec pede e que o plano trata de propósito num lugar diferente:** a spec fala em "o motor confere que as colunas são distintas". O plano põe a conferência do motor só no caso `multi_artes` (Task 4) e a do pedido inteiro no painel (Task 7), porque nenhum dos dois enxerga o quadro sozinho — foi o ajuste feito na auto-revisão da própria spec.

**Nomes conferidos entre tarefas:** `PoolQR.codigo` / `PoolQR.conteudo` / `PoolQR.fechar` (Task 1) são os mesmos usados nas Tasks 3 e 5; `coluna_do_modelo` e `indice` (Task 1) aparecem nas Tasks 2, 4 e 5; `_conteudo_qr_ideal` e `_modelo_do_item` (Task 3) são consumidos pela Task 4; `window.colunaQrIdeal` e `window.conferirColunasQrIdeal` (Task 7) batem com o harness do teste; `_qr_ideal_conteudo` é a chave injetada na Task 3 e lida no mesmo lugar.
