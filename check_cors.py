import asyncio; from local_print_agent import app; from starlette.requests import Request; from starlette.datastructures import Headers; mw = app.user_middleware[0].cls(app, **app.user_middleware[0].kwargs); r = mw.preflight_response(Headers({'origin': 'https://imposicao.vercel.app', 'access-control-request-method': 'GET', 'access-control-request-private-network': 'true'})); print(r.status_code, r.body)
